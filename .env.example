# ======================================
# GitHub Actions Secrets (set in GitHub UI)
# ======================================
# Required: none (workflow uses default GITHUB_TOKEN)
# Optional (ArgoCD auto-sync):
#   ARGOCD_SERVER=https://argocd.example.com
#   ARGOCD_TOKEN=ARGOCD_AUTH_TOKEN
# Optional (if repo requires separate PAT for pushes):
#   GH_PAT=ghp_xxx

# ======================================
# Kubernetes Sealed Secrets Templates
# Use ./scripts/seal-secrets.sh to generate encrypted manifests.
# ======================================

# backend-secret
DATABASE_URL=postgres://deuseda:CHANGE_ME@postgres:5432/deuseda_console
JWT_SECRET=CHANGE_ME_RANDOM_64_CHARS

# postgres-secret
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
